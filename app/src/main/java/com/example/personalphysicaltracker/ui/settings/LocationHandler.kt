package com.example.personalphysicaltracker.ui.settings


object LocationHandler {

    /*
    private lateinit var locationManager: LocationManager
    private lateinit var client: FusedLocationProviderClient
    public lateinit var map: MapView
    fun initialize(context: Context, activity: Activity) {
        Configuration.getInstance()
            .load(context, activity.getPreferences(android.content.Context.MODE_PRIVATE))
        client = LocationServices.getFusedLocationProviderClient(activity)
        locationManager = context.getSystemService(Context.LOCATION_SERVICE) as LocationManager
        map = MapView(context)

    }

    @SuppressLint("MissingPermission")


    }
ù

     */
}
